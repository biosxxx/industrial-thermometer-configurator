# Local dev mount path used by `npm run dev` and Playwright.
# Leave empty to serve the app from `/app.html`.
# Example to emulate the main site shell path from a local repository:
# APP_DEV_MOUNT_PATH=/utility-apps/industrial-thermometer-configurator
APP_DEV_MOUNT_PATH=
